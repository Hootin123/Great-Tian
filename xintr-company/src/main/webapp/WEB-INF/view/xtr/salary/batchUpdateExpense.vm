<!DOCTYPE html>
<html>
<head>
    <title>调薪</title>
    #parse("comm/head.vm")
    <link rel="stylesheet" type="text/css" href="#springUrl('/resource/js/plugins/laydate/need/laydate.css')"/>
    <script src="#springUrl('/resource/js/plugins/laydate/laydate.js')"></script>
    <style>
        .row{
            width: 550px;
            float:left;
            color: #333;
        }

        .row .divlabel{
            width: 40%;
            text-align: right;
        }

        .row > div{
            float:left;
            padding: 4px;
        }
        .row > div > label{
            line-height: 36px
        }
        input, select{
            padding: 0 5px;
            width: 278px;
            height: 34px;
            border: 1px solid #e3e3e3;
            position: relative;
            top: -1px;
            color: #666666;
            width: 175px;
        }
        .okbtn{
            display: block;
            width: 200px;
            height: 35px;
            line-height: 35px;
            background: #0696fa;
            color: #FFFFFF;
            border: none;
            float:left;
            cursor: pointer;
            text-align: center;
            border-radius: 6px;
            display: inline-block;
            font-family: "微软雅黑";
        }
        .cancelbtn{
            width: 198px;
            height: 33px;
            line-height: 33px;
            border: 1px solid #0696FA;
            color: #0696FA;
            background: #FFFFFF;
            cursor: pointer;
            float:right;
            text-align: center;
            border-radius: 6px;
            display: inline-block;
            font-family: "微软雅黑";
        }

        #foot{
            width:100%;
            height:35px;
            position:absolute;
            bottom:20px;
            padding:0;
        }
        .footBtn{
            width:410px;
            height:35px;
            margin:0 auto;
        }
        .top{
            text-align: center;
            padding-top:30px;
            color:#333;
        }
        .row1{
           width:100%;
            height:100px;
            color: #333;
        }
        .hint1{
            height:100%;
            line-height: 100px;
            padding-left:20%;
        }
        .hint2{
            width: 60%;
            height: 60px;
            padding-top: 40px;
            padding-left: 5px;
        }
        .row1 div{
            float:left;
        }
    </style>
</head>

<body>
<div>
    <div class="top"><h3>批量调额</h3></div>
    <form id="dataForm">
        <div class="row1">
            <div class="hint1">提示:</div>
            <div class="hint2">批量定额将会刷新现有报销额度。批量定额以后可针对个别员工情&nbsp;&nbsp;&nbsp;&nbsp;况进行调额。</div>
        </div>
        <div style="clear: both;padding: 0px"></div>

        <div class="row">
            <div class="divlabel">
                <label>
                    基本工资大于等于
                </label>
            </div>
            <div style="width: 200px;">
                <input id="gt" name="gt" value=""/>
            </div>
        </div>
        <div style="clear: both;padding: 0px"></div>

        <div class="row">
            <div class="divlabel">
                <label>
                    基本工资小于等于
                </label>
            </div>
            <div style="width: 200px;">
                <input id="lt" name="lt" value=""/>
            </div>
        </div>
        <div style="clear: both;padding: 0px"></div>

        <div class="row">
            <div class="divlabel">
                <label>
                    报销比例
                </label>
            </div>
            <div style="width: 200px;">
                <select name="bl">
                    <option value="30">30%</option>
                    <option value="40">40%</option>
                    <option value="50">50%</option>
                    <option value="60">60%</option>
                </select>
            </div>
        </div>
        <div style="clear: both;padding: 0px"></div>

        <div class="row">
            <div class="divlabel" style="width:130px;"></div>
            <div id="foot">
                <div class="footBtn">
                    <input class="okbtn btn" type="button" value="确定" onclick="UpdateSalaryExtend.save()">
                    <input class="cancelbtn btn" type="button" value="取消" onclick="UpdateSalaryExtend.close()">
                </div>
            </div>
        </div>
    </form>
</div>

</body>
<script>

    UpdateSalaryExtend = {
        save:function(){
            debugger;
            var gt = $("#gt").val();
            if(isNaN(gt) || (gt.indexOf(".") != -1 && gt != "")|| parseInt(gt) < 1){
                showWarning("请输入正确的参考数值");
                return;
            }

            var lt = $("#lt").val();
            if(isNaN(lt) || (lt.indexOf(".") != -1 && lt != "") || parseInt(lt) < 1){
                showWarning("请输入正确的参考数值");
                return;
            }
            if(lt == "" && gt == "") {
                showWarning("请输入正确的参考数值");
                return;
            }
            if(lt != "" && gt != "") {
                if(parseInt(lt) < parseInt(gt)) {
                    showWarning("请输入正确的参考数值");
                    return;
                }
            }
            $.ajax({
                type: 'post',
                url: BASE_PATH + '/expense/batchUpdateExpense.htm',
                data: $("#dataForm").serialize(),
                success: function(data) {
                    if (data.success) {
                        layer.alert('成功调额' + data.data + '人',{
                            title:'处理结果',
                        },function(index){
                            parent.onScreen();
                            UpdateSalaryExtend.close();
                        });

                    } else {
                        showWarning(data.message);
                    }
                }
            });

        },
        close:function(){
            var index = parent.layer.getFrameIndex(window.name); //获取窗口索引
            parent.layer.close(index);
        }
    }

</script>
</html>