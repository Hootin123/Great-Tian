<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xtr.core.persistence.reader.sys.SysMenuBtnReaderMapper">
    <resultMap id="BaseResultMap" type="com.xtr.api.domain.sys.SysMenuBtnBean">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="menu_id" property="menuId" jdbcType="BIGINT"/>
        <result column="btn_name" property="btnName" jdbcType="VARCHAR"/>
        <result column="btn_type" property="btnType" jdbcType="VARCHAR"/>
        <result column="action_urls" property="actionUrls" jdbcType="VARCHAR"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, menu_id, btn_name, btn_type, action_urls
    </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select
        <include refid="Base_Column_List"/>
        from sys_menu_btn
        where id = #{id,jdbcType=BIGINT}
    </select>


    <!-- 根据用户id查询按钮 -->
    <select id="getMenuBtnByUser" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT DISTINCT
        <include refid="Base_Column_List"/>
        FROM sys_menu_btn b
        WHERE EXISTS (
        SELECT * FROM sys_role_rel r
        WHERE r.obj_id = b.id AND r.rel_type = 2
        AND EXISTS (
        SELECT 1 FROM sys_role_rel u WHERE u.role_id = r.role_id AND u.rel_type = 1 AND u.obj_id =9
        )
        )
    </select>

    <!-- 根据菜单主键查询按钮-->
    <select id="getMenuBtnByMenuId" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT
        <include refid="Base_Column_List"/>
        from sys_menu_btn
        where menu_id = #{menuId,jdbcType=BIGINT}
    </select>

    <!-- 查询所有按钮-->
    <select id="queryByAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_menu_btn
    </select>

</mapper>